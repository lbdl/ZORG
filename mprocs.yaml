procs:
  katana:
    cwd: packages/contracts
    shell: katana --dev --dev.no-fee
  torii:
    cwd: packages/contracts
    shell: sozo build && sozo inspect && sozo migrate && sozo inspect && torii --http.cors_origins "*"
